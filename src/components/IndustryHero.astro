---
import { Image } from "astro:assets";
const { title, tagline, intro, image } = Astro.props;
---

<section
    class="relative bg-white-200 text-white-200 py-10 md:py-16 shadow-sm md:shadow-md"
>
    <div class="container">
        <!-- Hero Wrapper with Pill Feel -->
        <div
            class="relative w-full overflow-hidden rounded-[2rem] shadow-xl border border-gray/10"
        >
            <!-- Optimized Background Image -->
            {
                image && (
                    <Image
                        src={image}
                        alt={title}
                        class="absolute inset-0 w-full h-full object-cover object-center"
                        widths={[640, 1024, 1440, 1920]}
                        sizes="100vw"
                        format="webp"
                        loading="lazy"
                        decoding="async"
                    />
                )
            }

            <!-- Stronger Overlay Gradient -->
            <div
                class="absolute inset-0 bg-gradient-to-tr from-black-200/80 via-gray/80 to-black-200/80"
            >
            </div>

            <!-- Content -->
            <div class="relative z-10 px-6 md:px-12 lg:px-20 py-12 md:py-20">
                <div
                    class="max-w-2xl md:max-w-3xl lg:max-w-4xl text-center md:text-left"
                >
                    <!-- Title -->
                    <h1
                        class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-extrabold leading-tight mb-3 drop-shadow-lg"
                    >
                        {title}
                    </h1>

                    <!-- Tagline -->
                    {
                        tagline && (
                            <h2 class="text-lg sm:text-xl md:text-2xl lg:text-3xl font-medium leading-snug mb-4">
                                {tagline}
                            </h2>
                        )
                    }

                    <!-- Intro Paragraph -->
                    {
                        intro && (
                            <p class="text-sm sm:text-base md:text-lg leading-relaxed max-w-2xl">
                                {intro}
                            </p>
                        )
                    }
                </div>
            </div>
        </div>
    </div>
</section>
