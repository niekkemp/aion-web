---
const { bg = false } = Astro.props; // now boolean, not string
---

<section
    class={`overflow-hidden py-14 md:py-20 ${bg ? "bg-white-200 shadow-sm md:shadow-md" : ""}`}
>
    <div class="space-y-12 container">
        <slot />
    </div>
</section>
